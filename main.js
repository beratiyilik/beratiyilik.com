/**
* main.js
*/
; (function () {
    'use strict';

    function init() {
        window.location.pathname = '/cv'
    }

    window.addEventListener('load', function (event) {
        init();
    });
})();